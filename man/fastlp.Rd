% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fastlp.R
\name{fastlp}
\alias{fastlp}
\title{A generic LP solver}
\usage{
fastlp(obj, mat, rhs, lambda = 0)
}
\arguments{
\item{obj}{The objective vector of the coefficient with length \code{n}.}

\item{mat}{The constraint matrix of the linear programming with dimension
\code{m} by \code{n}.
Note this argument must be in matrix form even it is a vector.}

\item{rhs}{The right hand side vector of the constraint with length \code{m}.}

\item{lambda}{The parametric simplex method will stop when the calculated
parameter is smaller than \code{lambda}.
The default value is \code{0} and it corresponds to the optimal value.}
}
\value{
The optimal value will be returned if it exists.
  Otherwise the function will indicate the problem is infeasible or
  unbounded.
}
\description{
A generic linear programming solver using parametric simplex method.
}
\details{
This function is used to solve a general linear programming in standard
inequality form.
\deqn{\max obj^T x \quad subject\ to: mat \times x \le rhs, x \ge 0}
}
\note{
The linear programming should be in the form \eqn{\max obj^T x}
  subject to \eqn{mat \times x \le rhs}, \eqn{x \ge 0}.
  If the original problem is not in this form, the user has to convert it
  into this form.
  For example, the equality constraints can be separated into two inequality
  constraints.
}
\examples{
# Generate an LP problem and solve it
A <- matrix(c(-1, -1, 0, 1, -2, 1), nrow = 3)
b <- c(-1, -2, 1)
c <- c(-2, 3)
fastlp(c, A, b)
}
\seealso{
\code{\link{fastclime}} and \code{\link{fastclime-package}}
}
\author{
Haotian Pang, Han Liu and Robert Vanderbei \cr
  Maintainer: Haotan Pang <hpang@princeton.edu>
}
