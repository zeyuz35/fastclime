## 2026-03-27 - Missing sd binding in NAMESPACE\n**Learning:** R CMD check revealed that `sd` was missing from the `importFrom("stats", ...)` in the manually maintained NAMESPACE file, causing a NOTE. Adding it resolved the issue.\n**Action:** Always check the manually maintained NAMESPACE file for missing standard function bindings when adding new functions (like fastclime.BK17 and ZKL15).
