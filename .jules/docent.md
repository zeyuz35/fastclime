## 2026-05-26 - [Documentation Style Check]
**Learning:** Informal exclamation points were used in messages and errors across multiple R source files (`R/fastclime.R`, `R/fastlp.R`, etc.) and test assertions. These required removal. Structural hygiene items (non-R data in `data/`, namespace exports for standard utility functions like `flush.console`, and stale `build/vignette.rds`) can block passing checks and require updating alongside doc fixes.
**Action:** Replace `Done!`, `optimal solution found!`, `The problem is infeasible!`, `The problem is unbounded!` and `Some columns do not reach the required lambda!` with standard terse responses ending with a period. Move non-R `.md` files to `inst/extdata/` to avoid data directory warnings, delete stale check caches (`build/vignette.rds`), and explicitly import implicit globally assumed utils functions (`flush.console`).
